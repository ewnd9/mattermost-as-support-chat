{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start:dev": "tsx -r dotenv/config src/index.ts",
    "build": "rm -rf dist && tsc"
  },
  "dependencies": {
    "@trpc/server": "^10.9.0",
    "dotenv": "^16.0.3",
    "envalid": "^7.3.1",
    "fastify": "^4.11.0",
    "fastify-metrics": "^10.0.2",
    "zod": "^3.20.2"
  }
}
